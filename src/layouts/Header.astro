---
import { homeUrl, localeFromUrl, useLocale } from '@utils/locale'
import { SITE_NAME } from '@utils/publishing'
import ThemeSelect from '../components/ThemeSelect.astro'
import LanguageSelect from '../components/LanguageSelect.astro'

const locale = localeFromUrl(Astro.url)
const home = homeUrl(locale)
const { t } = useLocale(Astro.url)
---

<header class="site-header">
  <div class="site-header__row">
    <a href={home} class="site-title">
      <span class="visually-hidden">{t('pagination.all_posts')}: </span>
      <span class="site-title__text">{SITE_NAME}</span>
    </a>
    <div
      class="site-controls"
      role="group"
      aria-label={t('controls.display_preferences')}
    >
      <LanguageSelect />
      <ThemeSelect />
    </div>
  </div>
</header>
